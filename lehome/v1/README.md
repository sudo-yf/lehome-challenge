# LeHome v1 备份

这里保存的是原始 `start/` 目录中的旧脚本备份：
- `step1.sh`
- `step2.sh`
- `step3.sh`
- `step_git.sh`
- `step_vpn.sh`

这些文件保留的目的主要是：
- 回看旧版流程
- 对照新旧入口差异
- 必要时做手工排查

默认情况下，不建议继续把 `v1/` 作为主入口使用；当前主力入口已经切换到 `lehome/`。
